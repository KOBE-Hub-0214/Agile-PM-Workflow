# 工作流定义文件

工作流的核心定义已统一到 **`../agile-pm-workflow_skill/SKILL.md`**。

如果你是通过 AI IDE Skill 系统使用，请直接调用 `/agile-pm-workflow`。

如果你是手动使用，请将 `../agile-pm-workflow_skill/SKILL.md` 的内容发送给你的 AI 助手。

---

## 为什么合并？

之前 `pm_workflow_definition.md` 和 `agile-pm-workflow_skill/SKILL.md` 的内容几乎一致（仅 frontmatter 不同），维护两份容易导致内容不同步。现在统一为单一源文件。
